import { Stack } from "expo-router";

/**
 * Root application layout.
 */
const RootLayout = () => (
  <Stack
    screenOptions={{
      headerShown: false,
    }}
  />
);

export default RootLayout;
